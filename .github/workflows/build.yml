name: Build Python to EXE

on:
  push:
    branches: [main]

jobs:
  build-windows-exe:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install PyInstaller
      run: pip install pyinstaller
      
    - name: Build EXE
      run: |
        pyinstaller --onefile --console --name MyApp src/main.py
      
    - name: Upload EXE
      uses: actions/upload-artifact@v4
      with:
        name: Windows-Executable
        path: dist/MyApp.exe
